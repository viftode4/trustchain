<!-- OMX:AGENTS-INIT:MANAGED -->
<!-- Parent: ../AGENTS.md -->
# trustchain

This AGENTS.md scopes guidance to `trustchain`. Parent AGENTS guidance still applies unless this file narrows it for this subtree.

## Bootstrap Guardrails
- This is a lightweight scaffold generated by `omx agents-init`.
- Refresh updates the layout summary below and preserves the manual notes block.
- Keep only directory-specific guidance here; do not duplicate the root orchestration brain.

## Current Layout

### Files
- `.gitignore`
- `.mcp.json`
- `Cargo.lock`
- `Cargo.toml`
- `CLAUDE.md`
- `docker-compose.yml`
- `Dockerfile`
- `install.sh`
- `LICENSE`
- `README.md`
- `test_vectors.json`

### Subdirectories
- `.cargo/`
- `.github/`
- `.omc/`
- `.ruff_cache/`
- `.serena/`
- `deploy/`
- `proto/`
- `trustchain-core/`
- `trustchain-node/`
- `trustchain-transport/`
- `trustchain-wasm/`

<!-- OMX:AGENTS-INIT:MANUAL:START -->
## Local Notes
- Read `CLAUDE.md` here before making non-trivial changes; it is the detailed Rust-side guide.
- Verification: `cargo build`, `cargo test --workspace`, `cargo clippy --workspace --all-targets -- -D warnings`, `cargo fmt --all`.
- Keep timestamps as `u64` milliseconds, preserve canonical JSON hashing, and do not change `GENESIS_HASH`, `GENESIS_SEQ`, or `UNKNOWN_SEQ`.
- `BlockStore` must remain `Send` but not `Sync`; do not add a `Sync` bound.
- Any wire-format, block-field, hash, or delegation-constant change requires matching updates in `../trustchain-py` and `../trustchain-js`.
<!-- OMX:AGENTS-INIT:MANUAL:END -->
